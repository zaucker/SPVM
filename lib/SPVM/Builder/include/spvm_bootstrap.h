#ifndef SPVM_BOOTSTRAP_H
#define SPVM_BOOTSTRAP_H

#include "spvm_native.h"

// These functions is boot strap functions
// Never change function definitions after major release
SPVM_ENV* SPVM_BOOTSTRAP_new_env();

#endif
